Add a numeric separate-chaining hash map represented by bucket-head handles plus parallel indexed node arrays (keys, values, next handles), using -1 as null. Create a genuine mini-app that solves a simple collision-heavy problem and demonstrates insert, lookup, duplicate update, deletion from bucket head and interior, negative keys, stored zero, deterministic traversal/output, retained immutable versions, load-driven resize, and full rehash if practical. Add mlplunit coverage for empty lookup, forced collisions, chain invariants, head/interior deletion, missing deletion, duplicate update without size growth, collision preservation across resize, old-version retention, and malformed-handle validation. Prefer recursive pure helpers and zero explicit loops; document logical complexity versus immutable array-copy cost and application-managed handle/memory constraints. Register scripts, update README and plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.