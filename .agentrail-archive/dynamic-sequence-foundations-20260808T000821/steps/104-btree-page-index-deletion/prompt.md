Extend the immutable order-three B-tree page index in this repository only with persistent deletion. Implement leaf deletion, internal-key replacement by predecessor or successor, sibling borrowing/rotation, sibling merge, underflow propagation, and root contraction while preserving the existing duplicate/search policy and retained roots. Add a genuine page-index retirement mini-app rather than an assertion script. Cover deletion from one/two-key leaves, internal keys, borrow from left/right, merge on left/middle/right paths, cascading merge/root shrink, missing deletion, deletion to empty, negative and zero keys/values, all invariants, and retained prior versions. If the fixed 2-3 representation makes a correct operation impractical under current MLPL, document the exact blocker and choose another currently implementable non-memory demo rather than shipping incomplete deletion. Prefer recursion/composition with no explicit loops, give every UDF a doc string, document logical complexity and current copy costs, add focused native mlplunit tests, catalogs, README/docs/counts/sample reports, run just check, commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.