Add a numeric union-find/disjoint-set mini-app with immutable parent and rank/size vectors, recursive find, union by rank, and path-compression evidence. Solve a practical undirected connectivity problem such as grouping network links or accounts. Define whether find returns an updated structure plus root so compression remains pure. Add mlplunit coverage for empty/invalid index policy, singleton, initially separate sets, repeated/idempotent union, union by rank, path compression, connected queries, multiple components, negative/out-of-range IDs, retained old versions, representation invariants, and deterministic roots under ties. Compare component results with a small graph reachability fixture if practical. Prefer recursion and zero explicit loops; document amortized logical behavior versus immutable full-vector copying and how transients/COW could improve it. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.