Extend the tombstone-capable numeric open-address map with load-factor-driven growth and full rehash into a larger table. Create a genuine mini-app that grows from a small capacity while preserving colliding keys, updates, stored zero, and deletions. Add mlplunit coverage for threshold boundary, resize trigger, new capacity, rehash lookup parity, tombstone cleanup, collision/wraparound after resize, duplicate update without resize, negative keys, invariants, retained old versions, and repeated growth if practical. Prefer recursion with zero explicit loops, document logical amortized behavior versus current immutable copying, register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.