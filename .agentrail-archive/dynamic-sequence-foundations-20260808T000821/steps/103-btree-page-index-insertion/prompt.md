Add a genuine page-oriented numeric index mini-app using an immutable small-order B-tree in this repository only. Implement search and persistent insertion with leaf/internal splitting and root growth; choose and document a fixed minimum degree or maximum key count suitable for clear MLPL code. Use numeric keys and values, define duplicate replacement, and retain an earlier index version. Cover empty/singleton trees, ordered and adversarial insertion, leaf and internal/root splits, duplicates, negative and zero keys/values, missing lookup, key-count/child-count/range/leaf-depth invariants, malformed nodes, and retained roots. Deletion may remain a separately planned operation, but document that boundary. Prefer recursion/composition with no explicit loops, give every UDF a doc string, distinguish logical logarithmic page traversal from current nested-record copy costs and unguaranteed structural sharing, add focused native mlplunit tests, catalogs, README/docs/counts/sample reports, run just check, commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.