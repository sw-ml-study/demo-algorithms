Add a genuine editable-route mini-app using an immutable indexed doubly linked list in this repository only. Implement shared source with numeric handles and parallel values/previous/next/active arrays, plus head/tail/size. Cover insertion at head, before/after middle, and tail; deletion at head, middle, and tail; forward and reverse traversal; safe lookup by handle; reciprocal-link, active-handle, endpoint, size, and bounded-cycle validation; retained immutable versions; duplicate and negative values; empty/singleton behavior; and invalid/stale handles. Use no explicit loops, document logical complexity and current array-copy amplification, give every UDF a doc string, and build a real problem-solving route-editing demo rather than an assertion script. Add focused native mlplunit tests, catalogs, README/docs/counts/samples; run just check; commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.