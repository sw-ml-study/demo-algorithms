Reassess the six remaining GoF patterns against the current sw-MLPL binary, keeping all edits in this repository and adjacent repositories read-only. Implement an honest constrained functional Builder demo now if explicit pure staged transformations can establish Builder intent without record-update syntax; use an immutable configuration, validate required stages, retain earlier versions, and clearly distinguish it from Factory Method. Add shared doc-stringed source, a result-oriented zero-loop demo, focused native mlplunit tests, catalogs, docs/counts, regenerated reports, and full validation. For Singleton, Facade, Chain of Responsibility, Mediator, and Observer, update the evidence matrix with the exact smallest gate and identify whether any honest fixed-schema acceptance can proceed today without mislabeling central opcode dispatch or include conventions as the pattern. Plan the next executable step accordingly.