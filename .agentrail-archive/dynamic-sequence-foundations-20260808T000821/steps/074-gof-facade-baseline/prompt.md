Implement an honest constrained functional Facade demo with current sw-MLPL, editing only this repository and treating adjacent repositories as read-only. Choose a concrete general-purpose subsystem already represented by multiple operations or build a small tested numeric subsystem. Expose one simplified orchestration function that coordinates at least three subsystem operations, propagates Result errors, retains inputs, and returns effects as data where relevant. Keep subsystem helpers independently testable and clearly state that include does not enforce privacy; modules/private exports gate the preferred library boundary but not Facade intent. Add doc-stringed shared source, a result-oriented zero-loop demo, focused native mlplunit tests, catalogs, docs/counts, regenerated human/TAP samples, and full validation. Then select fixed nested Chain as the next acceptance experiment if honest delegation is feasible.