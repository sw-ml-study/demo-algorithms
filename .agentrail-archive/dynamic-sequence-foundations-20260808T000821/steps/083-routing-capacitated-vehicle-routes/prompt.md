Add a genuine capacitated vehicle-routing mini-app using current sw-MLPL without modifying adjacent repositories. Model one depot, numeric customer demands, vehicle capacity, and a weighted distance matrix. Implement a deterministic small-instance exact or clearly bounded solver plus a practical greedy route-construction comparison; return multiple closed routes, total cost, and served-customer invariants. Cover infeasible demand, disconnected edges, capacity boundaries, deterministic ties, retained inputs, and agreement with a small oracle. Prefer pure recursion, immutable numeric arrays/records, and zero explicit loops. Clearly distinguish CVRP from TSP and document logical complexity and immutable copying costs. Update catalogs/docs/counts/sample reports, validate, commit, push, and identify language features that would simplify nested route collections and pruning.