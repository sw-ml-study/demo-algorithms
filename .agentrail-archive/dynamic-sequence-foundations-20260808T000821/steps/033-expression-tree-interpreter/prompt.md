Add a tagged numeric expression-tree mini-app as a closed Composite/Interpreter baseline. Represent literals and a fixed set of arithmetic operators with numeric tags and immutable nested records or an indexed arena; recursively evaluate and produce deterministic traversal/token output without relying on strings as data. Solve a practical numeric formula problem. Add mlplunit coverage for literals, nested operators, precedence encoded by shape, negative and zero values, each supported operator, division-by-zero or invalid-tag errors, malformed arity/shape if representable, retained shared-looking immutable subtrees, and evaluation/traversal parity. Prefer pure recursion and zero explicit loops. Clearly document why this demonstrates closed Composite and Interpreter behavior but does not provide open extensibility without first-class functions, tagged unions/pattern matching, protocols, or modules. Register scripts, update README/plans/design-pattern capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.