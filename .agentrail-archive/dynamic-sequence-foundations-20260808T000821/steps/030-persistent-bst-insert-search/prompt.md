Add a persistent numeric binary-search-tree mini-app using immutable nested records and recursive search/insert. Solve a practical problem such as maintaining a numeric reservation or inventory index while retaining an earlier snapshot. Define and test a duplicate-key policy, negative keys, stored zero values if key/value pairs are used, empty/singleton/balanced/skewed cases, inorder ordering, successful/missing search, insertion at root/leaf/internal paths, old-root validity, BST invariants, and recursion-depth/copy-cost behavior. Prefer path rebuilding, pure functions, and zero explicit loops. Compare logical O(height) allocation with current sw-MLPL nested-record copying and identify the structural-sharing/runtime behavior needed for true Clojure-style persistence. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.