Resume non-GoF algorithm work with a genuine A-star shortest-path routing demo over the existing weighted numeric graph representations, editing only this repository and treating adjacent repositories as read-only. Reuse shared graph production sources where practical. Use an admissible numeric heuristic fixture, deterministic tie policy, predecessor/path reconstruction, and cross-check the result against existing Dijkstra on the same nonnegative graph. Cover zero heuristic equivalence, unreachable target, invalid input/negative weights, retained graph inputs, and a case showing fewer or equal expanded nodes than Dijkstra. Add doc-stringed shared source, a result-oriented zero-loop demo, focused native mlplunit tests, catalogs, plan/analysis/README updates, regenerated human/TAP samples, and full validation. Document current recursion/copy costs and the UDF-combinator or priority-queue improvements that would make the implementation more general.