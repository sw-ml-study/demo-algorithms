Added a pure recursive top-down Held-Karp exact TSP solver over immutable flat memo tables keyed by visited subset and current city. It matches factorial exact search's optimum and ascending first-minimum tour, reconstructs fixed-start closed tours, reports memoized states, retains inputs, rejects malformed/disconnected/nonintegral inputs, and caps exponential allocation at 12 cities. The visible TSP app now compares factorial exact, Held-Karp, nearest neighbor, and 2-opt. Documented O(V^2*2^V) time/O(V*2^V) state and the need for bit sets, general maps, state folds, and transient/COW tables. Reorganized README Status into readable tool, corpus, roadmap, algorithm, and pattern groups. Validation passes with 72 demos, 114 tests, and 627 documented UDFs.