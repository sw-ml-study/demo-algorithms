Extend the fixed-capacity open-address numeric hash map with deletion and tombstone state. Create a genuine mini-app where removing one colliding key must not break lookup for later keys in the probe chain, and where a later insertion reuses the first tombstone safely. Add mlplunit coverage for remove hit/miss, chain preservation, tombstone reuse, duplicate update, wraparound, full/tombstoned tables, stored zero, size/invariants, retained versions, and invalid keys. Keep resizing separate. Prefer recursion with zero explicit loops, document probe termination and tombstone tradeoffs/current copy costs, register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.