Create demos/vectors/growable_vector.mlpl as the first dynamically sized data-structure demo. Provide a pure record API with data and logical size, append and pop operations returning new vectors/Results, empty-pop handling, representation validation, growth beyond the initial input, shrink behavior, and retained-old-version tests. Prefer concat and whole-array operations; use no explicit loops if possible. Document logical complexity and current copy complexity honestly. Register it in catalog/demos.tsv and run all harness/catalog/demo tests against the adjacent release binary. Do not modify ../sw-mlpl.