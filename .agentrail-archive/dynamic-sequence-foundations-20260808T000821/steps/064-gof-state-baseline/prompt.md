Continue the functional GoF baseline saga with a closed State case study. Build a genuine numeric problem-solving .mlpl mini-app where an immutable state record plus event transition returns the next state and explicit effects-as-data, with focused native mlplunit tests using shared src definitions. Separate transition logic from effect interpretation, demonstrate deterministic transitions, retained prior states, invalid events/transitions, and zero explicit loops. Be explicit that numeric tag dispatch is a closed baseline, not open delegated State: adding states/events edits central dispatch; the preferred extensible form requires first-class UDFs/function tables, variants, folds, modules, and record updates. Update catalogs, README, DESIGN_PATTERNS.md, analysis/plan/saga docs, and sample reports. Modify only this repository and run full validation.