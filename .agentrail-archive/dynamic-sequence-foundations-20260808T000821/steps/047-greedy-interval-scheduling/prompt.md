Add a genuine interval-scheduling mini-app that selects the maximum number of non-overlapping numeric intervals for a practical resource-booking problem. Normalize or validate parallel start/end/ID vectors, sort deterministically by earliest finish with start and ID tie-breakers, then greedily select compatible intervals and return the chosen IDs/times. Add mlplunit coverage for empty/singleton, disjoint, fully overlapping, touching endpoints policy, nested intervals, duplicate intervals/IDs policy, unsorted input, negative times, zero-length interval policy, malformed parallel vectors, invalid reversed intervals, deterministic ties, retained inputs, compatibility invariant, and a small known or brute-force optimum. Prefer recursion and zero explicit loops; include meaningful doc strings for every u: function; document O(n^2) insertion-sort baseline versus target O(n log n), immutable copy costs, and future modules/UDF comparators/builders. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run full validation, and modify only this repository.