Add weighted shortest-path mini-apps for dense Dijkstra and Bellman-Ford over a shared directed numeric edge fixture, with predecessor-based path reconstruction and cross-checks where both algorithms apply. Dijkstra must reject negative edges; Bellman-Ford must support them and report reachable negative cycles. Solve a practical routing problem and produce deterministic distances and one reconstructed route. Add mlplunit coverage for source-to-self, direct/multi-hop choice, unreachable vertices, zero-weight edges, equal-distance tie policy, cycles with nonnegative weights, Dijkstra negative-edge rejection, Bellman-Ford negative edge without cycle, reachable and unreachable negative cycles, predecessor/path reconstruction, malformed endpoints, retained graph versions, and distance parity on nonnegative fixtures. Prefer recursion and zero explicit loops where practical; account honestly for dense selection/relaxation and immutable copying. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.