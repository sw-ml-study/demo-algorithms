Extended TSP routing with deterministic recursive first-improvement 2-opt. It validates closed tours, enumerates segment reversals without explicit loops, repeatedly improves to local stability, preserves the seed, and rejects malformed/disconnected tours. The visible five-city fixture improves nearest neighbor from 62 to 52 in two swaps; exact factorial search confirms 52 is optimal. Full validation passes with 71 demos, 111 native tests from 65 files, and 611 documented UDFs.