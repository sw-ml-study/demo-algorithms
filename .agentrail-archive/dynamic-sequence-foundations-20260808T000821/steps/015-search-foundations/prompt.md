Begin the search-sort-priority saga with linear search, binary search, and lower bound. Create genuine numeric mini-app demos that solve simple problems, plus mlplunit conformance tests covering empty, singleton, absent, boundary, duplicate, and negative-value cases. Expose each algorithm rather than replacing it with a builtin oracle; prefer recursion or whole-array operations and account for every explicit loop. Register scripts, update README/plans and capability notes, inspect the then-current mlplunit interface before harness assumptions, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.