Add a persistent numeric AVL tree mini-app using immutable nested records with cached heights, recursive insert, balance-factor calculation, and single/double rotations. Solve a practical problem where ordered numeric entries arrive in an adversarial order but lookups must remain shallow while an earlier root remains valid. Define duplicate-key value replacement. Add mlplunit coverage for LL, RR, LR, and RL rotations; balanced and skew-inducing insertion sequences; negative keys; stored zero; duplicate update; inorder ordering; height consistency; balance bounds; search; old-root validity; and logarithmic-height evidence for a deterministic sequence. Prefer pure recursion and zero explicit loops. Document logical O(log n) behavior under AVL invariants versus nested-record copying, and the structural-sharing/GC requirements for physical persistence. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.