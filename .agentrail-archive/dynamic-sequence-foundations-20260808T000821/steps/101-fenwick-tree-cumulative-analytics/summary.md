Added an immutable Fenwick tree using a retained source vector plus compact one-based aggregate array, with bitwise-free lowbit calculation, persistent point-add updates, half-open prefix/range sums, recursive construction, and validation by rebuilding internal aggregates. Added a genuine cumulative-shipment correction mini-app comparing live totals with an audit version and explicitly documenting whole-array copy amplification. It complements rather than duplicates the segment tree: Fenwick owns compact sum analytics, while segment tree retains range minimum. Three native mlplunit cases cover empty/singleton/non-power-of-two vectors, signed/zero values, zero/interior/full prefixes, point/range queries, positive/negative updates, bad shapes/indices/ranges, internal corruption, and retained versions. Updated catalogs, README, analysis, plan, counts, and human/TAP reports. Full just check passed: 83 demos, 77 test files, 148 native tests/cases, and 809 documented UDFs.