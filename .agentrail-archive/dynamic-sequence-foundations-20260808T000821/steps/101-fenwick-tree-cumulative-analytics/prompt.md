Add a genuine cumulative-analytics mini-app using an immutable Fenwick tree (binary indexed tree) in this repository only. Build from a numeric vector, support persistent point-add updates, prefix sums, and half-open range sums, and compare its representation/operations honestly with the segment-tree demo without duplicating range-minimum behavior. Retain an earlier version for audit comparison. Cover empty/singleton/non-power-of-two lengths, negative and zero values, zero/full/interior prefixes, point and range queries, invalid indices/ranges, internal-tree validation against source values, and retained versions. Prefer recursion/composition with no explicit loops, give every UDF a doc string, document logical O(log n) operations versus current immutable array-copy amplification, add focused native mlplunit tests, catalogs, README/docs/counts/sample reports, run just check, commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.