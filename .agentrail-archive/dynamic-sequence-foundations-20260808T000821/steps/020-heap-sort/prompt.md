Add a genuine heap-sort mini-app and mlplunit conformance test by delegating ordering to the existing heap mechanism copied locally until modules/imports exist. Do not call a sorting builtin as the implementation. Solve a simple numeric ordering problem, cover empty, singleton, sorted, reverse sorted, duplicates, negatives, and input immutability; cross-check output with a suitable builtin oracle only if useful. Prefer recursion, account for loops, document logical/current copy complexity and the duplicated-helper module pressure, register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.