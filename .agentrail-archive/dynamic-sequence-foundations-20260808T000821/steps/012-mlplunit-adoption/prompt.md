Adopt ../../softwarewrighter/mlplunit as far as possible without modifying mlplunit or sw-MLPL. First prove a representative campaign-goal test using the existing mlplunit binary and assertion prelude. Integrate mlplunit behind the catalog-driven scripts/run-tests with MLPLUNIT override, PATH lookup, and adjacent development fallback while preserving metadata/gated behavior. Rename conformance files to test_*.mlpl for native discovery, remove duplicated local assertion helpers, migrate assertions to mlplunit APIs, and keep algorithm helpers local until includes/modules are justified. Update harness, README, catalogs, and plans. Run every test in isolated mlplunit processes plus all demos. Record concrete upstream mlplunit or sw-MLPL gaps; do not modify adjacent repositories without separate authorization.