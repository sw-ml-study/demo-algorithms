Added a genuine stable delivery-ordering mini-app using recursive merge sort over parallel ETA and delivery-ID vectors with zero explicit loops. Implemented mask-based halves and index-bound merging, choosing the left payload on ties to preserve dispatch order across recursive halves. Added eleven mlplunit cases covering empty, singleton, sorted/reverse, odd/even, duplicates with stability, negatives, and payload alignment. Documented logical O(n log n) behavior versus immutable allocation overhead, registered the twelfth demo/fifteenth test, updated README/plans, and passed the full validation matrix.