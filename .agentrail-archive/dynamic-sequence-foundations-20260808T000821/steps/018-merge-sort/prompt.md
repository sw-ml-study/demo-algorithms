Add a genuine stable merge-sort mini-app and mlplunit conformance test using index bounds or masks rather than unavailable slice syntax. Expose recursive divide and stable merge; do not use grade_up as the implementation. Solve a simple numeric ordering problem with parallel payload IDs if practical, cover empty, singleton, sorted, reverse sorted, odd/even lengths, duplicates with stability evidence, and negative values. Document logical/current copy complexity, recursion, and loop accounting; register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.