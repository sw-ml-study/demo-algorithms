Investigate and implement honest fixed-schema functional Observer and Mediator acceptance demos with current named UDF references and fixed callable records, editing only this repository and treating adjacent repositories as read-only. Observer must invoke at least two independent subscriber behaviors, return each subscriber's new state/effects as data, retain prior states, and avoid pretending fixed fields are dynamic subscription. Mediator must coordinate at least two callable participant transitions through an explicit mediator function while keeping participants from calling each other directly; avoid a central opcode switch that embeds participant behavior. If either cannot be honest with fixed records, document its exact gate and continue with the other. Add doc-stringed shared sources, result-oriented zero-loop demos, focused native mlplunit tests, catalogs, docs/counts, regenerated reports, and full validation. Clearly distinguish fixed evidence from dynamic forms gated on callable registries, folds, general state maps, variants, and modules.