Create an index-backed singly linked-list conformance test under tests/linked_lists and a genuine mini-app under demos/linked_lists that solves a simple delivery-route insertion problem: insert an urgent stop after a specified existing stop without shifting other logical nodes, then traverse and print the resulting route. Use numeric stop IDs, parallel value/next arrays with -1 null and a head handle, application-managed indices, and current sw-MLPL only. Explain why a linked list fits. Keep assertions in the test; cover invariants, traversal, insert-after at head/middle/tail, missing target, dynamic node growth, and persistence. Register both, update README, run all suites, keep helpers local until modules/imports land, and do not modify ../sw-mlpl.