Add a meaningful leading doc-string to every user-defined u: function in every demo and test .mlpl script so :fns, :describe, and :list expose function intent. Cover the entire existing corpus, including repeated copied-local helpers and test entry points. Preserve behavior and standalone scripts; do not modify adjacent repositories. Add an automated repository audit that fails when any u: definition lacks a leading string doc-string, integrate it into the validation workflow, document the convention in README and contributor/plan guidance, run all demos/tests/mlplunit/harness plus the new audit, and modify only this repository.