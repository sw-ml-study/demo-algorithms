Added deterministic pure-recursive Edmonds-Karp maximum flow over flat capacity, residual, and signed net-flow matrices. Recursive immutable-queue BFS finds ascending shortest augmenting paths, predecessor reconstruction computes bottlenecks, and paired forward/reverse updates expose rerouting. The warehouse-transfer mini-app solves the classic six-node network at maximum flow 23 in three augmentations with final path [0,2,4,3,5]. Native tests verify the oracle, source/sink totals, internal conservation, capacity bounds, deterministic paths, retained input, disconnected flow, pre-summed parallel capacity policy, and invalid shapes/capacities/endpoints. Documented O(V*E^2) logical work and V^2 immutable matrix-copy amplification plus queue/fold/paired-update/transient improvements. Validation passes with 74 demos, 118 tests from 68 files, and 657 documented UDFs.