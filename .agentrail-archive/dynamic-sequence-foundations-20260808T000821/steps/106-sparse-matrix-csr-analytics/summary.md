Added general-purpose numeric COO and CSR sparse matrices, separate from graph traversal. Deterministic COO-to-CSR conversion validates rectangular dimensions and coordinates, sums duplicate coordinates, elides explicit/resulting zeros, and emits sorted columns. Added CSR validation with safe short-circuiting, sparse matrix-vector multiplication, zero-default cell lookup, and rectangular transpose. Added a genuine sparse inventory projection mini-app and three native mlplunit cases covering empty/singleton/rectangular matrices, unsorted duplicates, signed/zero values, dense matvec/cell oracles, retained COO inputs, shape/dimension errors, and pointer/column corruption. All operations use recursion and zero explicit loops; docs state that conversion/transpose scan coordinate domains and immutable concat amplifies copies. Updated catalogs, README, analysis, plan, counts, and human/TAP reports. Full just check passed: 88 demos, 82 test files, 163 native tests/cases, and 916 documented UDFs.