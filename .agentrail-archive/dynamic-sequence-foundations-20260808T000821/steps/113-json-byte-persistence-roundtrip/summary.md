Verified fresh sw-MLPL 0.20.0 binary/source 899631aa and implemented the unblocked persistence wave. Added total type_of root validation to JSON dispatch ingestion; a deterministic JSON plan save/load/compare/cleanup mini-app; a versioned checksummed seven-byte device-command save/load/validate/cleanup mini-app; shared JSON and binary persistence sources; focused mlplunit tests for root rejection, deterministic JSON, exact raw bytes, corruption and field bounds; catalogs, README, and acceptance/status/plan updates. Kept higher-rank JSON decode, semantic Result decoding, non-finite policy, numeric-f64 byte representation, hard byte-cell validation, non-atomic writes, TOML, native formats, streaming, and decode limits explicitly gated. just check passes with 92 demos, 85 test files, 174 native tests, and 947/947 documented UDFs.