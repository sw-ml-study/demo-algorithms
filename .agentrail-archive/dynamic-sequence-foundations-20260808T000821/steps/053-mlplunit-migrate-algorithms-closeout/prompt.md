Finish full native mlplunit adoption for dynamic-programming and greedy algorithm-survey files, then audit the whole repository. Extract shared production definitions under src/, make demos/tests include them, remove copied implementations, and use focused named/tagged @test plus useful @cases. Add mechanical adoption checks proving every registered test delegates to mlplunit, every applicable demo/test pair shares production source, no monolithic u:test entry points or copied assertion/lifecycle frameworks remain, all functions have doc strings, and config discovery/catalog selection agree. Exercise skip/expected-failure/parameter/bracket capabilities only through honest small contract fixtures if the production corpus has no natural case; do not force lifecycle into immutable algorithms. Update README, analysis, plan, and migration inventory; run human and TAP suites, all demos, failure-continuation/exit checks, audits, and harness. Inspect but do not modify adjacent repositories; modify only this repository, then resume N-queens.