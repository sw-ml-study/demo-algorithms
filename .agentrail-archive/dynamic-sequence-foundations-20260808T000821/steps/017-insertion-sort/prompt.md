Add a genuine insertion-sort mini-app and mlplunit conformance test. Expose stable incremental insertion rather than calling grade_up as the implementation; grade_up may be used only as an oracle if useful. Solve a simple numeric ordering problem, cover empty, singleton, already sorted, reverse sorted, duplicates with stability evidence practical for numeric records/parallel arrays, and negative values. Prefer recursion and whole-array operations, document logical and current copy complexity plus loop accounting, register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.