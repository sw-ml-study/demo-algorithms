Add a genuine numeric-token longest-common-subsequence mini-app using dynamic programming, reconstructing one deterministic LCS without relying on strings. Solve a practical problem such as comparing numeric event traces or command sequences. Add mlplunit coverage for both empty, one empty, identical, disjoint, prefix/suffix, repeated tokens, multiple-optimum deterministic tie policy, symmetry of LCS length, retained inputs, reconstruction as a subsequence of both inputs, table dimensions/indexing, and a known optimum. Prefer recursion and zero explicit loops; document O(mn) work/space, recursion depth, immutable table-copy overhead, and future modules/folds/COW builders. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run full validation, and modify only this repository.