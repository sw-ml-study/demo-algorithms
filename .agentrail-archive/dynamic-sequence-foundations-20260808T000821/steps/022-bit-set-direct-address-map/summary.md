Added two genuine bounded associative mini-apps: feature flags backed by a bit-set mask and warehouse quantities backed by a direct-address map with separate presence/value arrays. Both use zero explicit loops, pure scatter updates, strict integral in-range key validation, and retained immutable versions. Added 28 mlplunit cases covering empty state, hit/miss, idempotent duplicate set/clear, duplicate-key update, stored zero, remove, bounds, negative/fractional/non-scalar keys, size/invariants, and fixed-universe constraints. Registered sixteen demos/nineteen tests, updated README/plans, and passed the full validation matrix.