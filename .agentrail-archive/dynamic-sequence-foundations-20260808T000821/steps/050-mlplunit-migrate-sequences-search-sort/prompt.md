Continue full native mlplunit adoption for vectors, stacks, queues, linked lists, persistent lists, search, sequences, sorts, and heaps. Extract reusable tested definitions to matching src/ files; make demos and tests include the same production sources; remove copied implementations from tests; replace monolithic/top-level test bodies with focused named/tagged @test functions and u:run_registered_tests(); use @cases for genuinely tabular numeric cases, and no fixtures unless lifecycle is real. Preserve catalog taxonomy, standalone demos under run-all --source-dir, all doc strings, policies, and behavior. Update migration inventory/docs, run human and TAP mlplunit, full demos, audits, and harness. Inspect but do not modify adjacent repositories; modify only this repository.