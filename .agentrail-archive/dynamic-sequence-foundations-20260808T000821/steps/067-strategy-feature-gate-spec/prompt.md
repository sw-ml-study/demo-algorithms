Prepare the next feature-gated acceptance step for functional Strategy without modifying sw-MLPL or any adjacent repository. Inspect the latest adjacent sw-MLPL documentation and binary read-only to determine whether named UDF values can now be passed and uniformly invoked. If supported, implement a genuine numeric Strategy mini-app and native tests that inject at least two comparator/scoring policies into one unchanged client. If still unsupported, do not create a fake opcode implementation: add a precise runnable acceptance design/gated catalog or documentation entry describing target MLPL syntax, substitutability tests, expected diagnostics, and the smallest sw-MLPL change needed. Refresh relevant status docs and samples only as appropriate, modify only this repository, and run full validation.