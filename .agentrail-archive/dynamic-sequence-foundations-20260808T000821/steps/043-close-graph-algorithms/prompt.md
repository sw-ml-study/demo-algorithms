Close the graph-algorithms saga with a cross-demo invariant and capability audit. Validate representation, traversal, cycle/topological, SCC, union-find, shortest-path, Floyd-Warshall, and Kruskal fixtures together where meaningful; add only focused cross-check tests needed to expose shared assumptions such as endpoint policy, reachability/component agreement, shortest-path row parity, DAG/cycle consistency, and MST forest edge-count invariants. Audit deterministic policies, explicit-loop counts, logical versus immutable-copy complexity, recursion limits, application-managed cycles, and copied-local helpers pending modules/includes. Update README, docs/analysis.md, docs/plan.md, and catalogs with final graph capability evidence and remaining sw-MLPL gaps. Inspect but do not modify adjacent repositories. Run the full validation matrix, modify only this repository, and queue the first algorithm-survey step when complete.