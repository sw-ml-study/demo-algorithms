Completed the selective combinator Strategy pilot without importing the sibling aviary. Added one general score_weighted(cost_weight,duration_weight,cost,duration) formula; progressive application binds weights into two-argument callable policies stored in records and accepted by the unchanged selector. Retained the three readable fixed policy aliases, delegating them to the general formula. Native tests prove configured balanced and fixed balanced policies are identical, a second urgent partial is substitutable, configuration records and partial values are retained, stable ties and validation remain unchanged. The demo visibly reports fixed/configured agreement. Measured result: formula sites 3->1, policy UDFs 3->4, so configurability and loose coupling improve but total lines/UDF count do not shrink. Validation passes with 76 demos, 126 tests from 70 files, and 691 documented UDFs.