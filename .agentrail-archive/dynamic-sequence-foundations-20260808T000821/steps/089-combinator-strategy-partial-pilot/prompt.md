Pilot the combinator recommendation in this repository only. Refactor or extend the existing shipping Strategy source so a general named weighted-score UDF can be partially applied into configurable callable policies, while preserving the existing selector, observable demo results, deterministic ties, validation, and native tests. Compare the partial-based form with the current fixed named policies; remove duplication only when clarity improves. Add focused tests proving partial values stored in policy records remain substitutable and retained configuration is immutable. Update the visible demo explanation and docs/combinator-refactoring.md with measured source/UDF clarity findings. Do not copy the full bird aviary or create a sibling dependency. Validate all demos/tests/docstrings/adoption, refresh counts/samples only if they change, commit, push, and decide from evidence whether to queue a pairwise table pilot.