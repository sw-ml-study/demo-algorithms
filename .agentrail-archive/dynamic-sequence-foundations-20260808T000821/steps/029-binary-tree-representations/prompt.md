Begin the persistent-tree-baselines saga by implementing and comparing record-based and index-backed numeric binary-tree representations. Create a genuine mini-app that models a small hierarchy and produces deterministic preorder, inorder, and postorder results using recursive traversal, with an iterative traversal only if it adds useful current-language evidence. Add mlplunit coverage for empty, singleton, balanced, skewed, duplicate numeric payloads, negative values, representation conversion/parity, malformed handles, logical cycles bounded by application-managed validation, and retained immutable versions. Prefer pure functions and zero explicit loops, document representation invariants, memory ownership/cycle constraints, recursion depth, immutable copying, and how future modules would share traversal helpers. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.