Add the minimal executable demo harness and catalog contract for standalone .mlpl scripts. Organize demos by data structure. Provide passing and intentionally failing fixtures proving that mlpl-repl final Ok succeeds and final Err exits nonzero. Add a runner using an MLPL environment override with an adjacent release-binary fallback. Validate catalog metadata including data_structure, algorithm, dynamic_size, explicit_loops, target_loops, and required_features. Test the harness against ../sw-mlpl/target/release/mlpl-repl. Do not modify ../sw-mlpl.