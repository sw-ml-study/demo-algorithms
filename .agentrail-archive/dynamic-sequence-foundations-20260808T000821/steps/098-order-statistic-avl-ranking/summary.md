Added a separately testable immutable order-statistic AVL set with cached height and subtree size, persistent duplicate-as-set insertion, all four AVL rotations, zero-based select, present/missing-key rank as sorted insertion position, inorder traversal, and recursive validation of caches, ordering, and balance. Added a genuine live-leaderboard mini-app that places score 140 and answers positional queries while retaining a published root and explicitly avoids claiming runtime structural sharing. Three native mlplunit cases cover rotations, corruption, empty/singleton trees, duplicates, negative/zero keys, select boundaries, rank semantics, bad inputs, and retained roots. Updated catalogs, README, analysis, plan, corpus counts, and human/TAP samples. Full just check passed: 80 demos, 74 test files, 139 native tests/cases, and 754 documented UDFs.