Using the current sw-MLPL binary c3452aa1, add TDD coverage and update the serialization workflow demo to exercise the shipped tagged-envelope round-trip contract for semantic Results and other supported tagged values. Preserve compatibility expectations honestly, update the catalog, README, serialization acceptance matrix, and current capability documentation, run the full pre-commit gate, and commit all source plus AgentRail metadata before completing the step. Do not implement typed-native binary serialization; document it as future work.