Begin the functional GoF baseline saga with Adapter. Build a genuine problem-solving .mlpl mini-app that adapts an existing graph representation through a pure conversion boundary, plus focused native mlplunit tests using shared src definitions. Make the adapted consumer depend only on the target record/array protocol, show retained source input and representation parity, use zero explicit loops where practical, and document logical/current copy complexity, recursion, limitations, and why this preserves Adapter intent without OO inheritance. Reuse existing graph conversion code rather than duplicating it; update catalogs, README, DESIGN_PATTERNS.md, analysis/plan/saga documentation, and sample reports. Modify only this repository and run full validation.