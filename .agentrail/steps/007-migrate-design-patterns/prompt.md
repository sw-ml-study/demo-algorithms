Move every design-pattern-owned demo, src definition, native test, DESIGN_PATTERNS.md material, GoF reports, and pattern acceptance/gate documents from demo-algorithms to demo-design-patterns. Preserve all 23-pattern status distinctions, shared source reuse in demos/tests, deterministic boundary coverage, explicit non-claims, catalog integrity, and a concise learner-first README. Do not create include-based web files; any web/*.mlpl must be standalone and runnable through Editor > Load > Run. Add failing-then-passing ownership/catalog/web assertions, run both full gates, commit each repository separately, push the target, and record exact commits.