Create a deque conformance test under tests/deques and a genuine mini-app under demos/deques that solves a simple service-desk scheduling problem: regular requests join the back, urgent requests join the front, and service removes from the front. Explain why a double-ended queue fits, use numeric request IDs, print a meaningful service order without assertions, and keep zero explicit loops if practical. The test must cover push-front/back, pop-front/back, empty operations, dynamic growth/shrinkage, invariants, and persistence. Register demo/test, update README, run all suites, keep helpers local until modules/imports land, and do not modify ../sw-mlpl.