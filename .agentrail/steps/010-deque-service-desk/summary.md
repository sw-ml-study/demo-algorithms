Added a genuine service-desk scheduling mini-app using an immutable dynamically sized deque: urgent request 999 enters at the front, regular requests enter at the back, and service preserves regular FIFO order. Added a separate twelve-case conformance test for both-end insertion/removal, empty/malformed cases, growth/shrinkage, invariants, and persistence; fixed an MLPL same-line else parser constraint and passed all suites.