Added deterministic minimum-cost square assignment with the standard Hungarian primal-dual work arrays expressed through pure recursion and immutable vectors. It returns zero-based inverse worker/task mates and total cost, supports negative and fractional costs, and defines equal-cost ties by ascending task scans and strict updates. A bounded factorial permutation oracle independently verifies fixtures. The technician/task mini-app finds [1,0,2,3] at cost 13, matching exhaustive search. Native tests cover known optimum, oracle and tie agreement, inverse uniqueness, retained input, equal costs, negative/fractional costs, singleton/empty policy, malformed shapes, invalid sizes, and oracle bounds. Documented O(N^3) work and immutable potential/slack/ownership copying plus fold/argmin/record-update/transient improvements. Validation passes with 76 demos, 125 tests from 70 files, and 689 documented UDFs.