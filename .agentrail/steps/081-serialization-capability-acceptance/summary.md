Audited current sw-MLPL serialization prerequisites read-only and published concrete JSON, TOML, and native-binary acceptance criteria covering strings versus bytes, sandbox/capability I/O, numeric types and shapes, variants, schema versions, deterministic ordering, errors and limits, streaming, endianness, callable codecs, shared references, and cycles. Added an honest versioned checksummed in-memory numeric envelope demo and native tests that round-trip scalars, empty vectors, vectors, and matrices without claiming JSON or durable binary I/O. Kept quant/model formats assigned to the future ML demo repository. Validation passes with 72 demos, 113 tests from 66 files, and 620 documented UDFs.