Audit current sw-MLPL read-only for general-purpose serialization prerequisites and update only this repository. Turn the existing JSON/TOML/binary serialization plan into concrete acceptance tests and a prioritized feature specification: strings versus byte vectors, file/capability I/O, numeric scalar/array/record type preservation, shapes/ranks, tagged variants, schema/version metadata, deterministic object field ordering, errors, streaming, endianness, and application-defined codecs. Identify what numeric in-memory encode/decode demo can run today without falsely claiming JSON/TOML or durable binary I/O; implement such a demo only if it solves a real problem and round-trips meaningful general-purpose numeric data. Keep quant/model formats explicitly in the separate future ML demo repository. Refresh docs/catalogs/tests as applicable, validate, commit, and push; never edit adjacent repositories.