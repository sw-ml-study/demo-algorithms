Add a Held-Karp dynamic-programming exact TSP implementation using current sw-MLPL, without changing adjacent repositories. Reuse the existing flat distance-matrix and closed-tour contracts, compare its optimum and deterministic tie policy against factorial exact search on small fixtures, and explain O(V^2*2^V) time/O(V*2^V) state versus factorial search. Build a genuine visible routing mini-app result or extend the existing TSP demo only if the distinction remains clear; add native mlplunit coverage for optimum, reconstruction, fixed start, disconnected/error boundaries, and retained inputs. Prefer pure recursion and immutable numeric arrays/records with zero explicit loops. Update catalogs/docs/counts/sample reports, validate, commit, push, and identify language features that would reduce immutable DP table copying.