Add a dense numeric binary min-heap and a genuine priority-queue mini-app that solves a simple scheduling problem by delegating enqueue/dequeue to heap operations. Add mlplunit conformance tests for empty/singleton, insertion, peek/remove-min order, duplicates, negative priorities, growth/shrinkage, heap invariants, and retained immutable versions. Expose sift-up/sift-down rather than using sort as the implementation; prefer recursion and account for loops. Document logical versus current copy complexity, register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.