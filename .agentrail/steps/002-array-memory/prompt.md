Create the first real data-structure demo at demos/vectors/array_memory.mlpl. Demonstrate a numeric vector as pure random-access memory: read, functional write, swap, bounds-safe Result API, representation invariants, and retained-old-value behavior. Keep it standalone and deterministic, document dynamic_size, representation, logical complexity, current copy complexity, explicit_loops, target_loops, and required feature gaps in header comments. Add it to catalog/demos.tsv under the vectors data structure. Test normal, empty/boundary, invalid-index, and persistence cases inside the script; end with Ok only if all invariants pass. Run tests/test-harness and scripts/run-all against the adjacent release binary. Do not modify ../sw-mlpl.