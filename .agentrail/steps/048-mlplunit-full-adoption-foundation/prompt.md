Begin full native mlplunit adoption using the read-only handoff at /Users/mike/github/softwarewrighter/mlplunit/docs/demo-algorithms-next.md. Add and validate mlplunit.conf; inspect current mlplunit and sw-MLPL include/reflection/bracket capabilities without modifying adjacent repositories; design and document the complete migration inventory; simplify scripts/run-tests so catalog selection delegates to mlplunit without duplicating lifecycle/reporting; keep tests/test-harness only for catalog/delegation behavior. Fully migrate a representative vertical slice (prefer deque/service_desk) by extracting production definitions to src/, making the demo include them, making tests include them, replacing monolithic u:test with named @test registry execution, using @cases where genuinely useful, and bracket only if lifecycle is real. Preserve all function doc strings and full validation. Queue bounded follow-up steps to migrate every remaining domain before resuming N-queens. Modify only this repository.