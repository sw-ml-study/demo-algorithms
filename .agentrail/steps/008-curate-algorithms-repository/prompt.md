Curate demo-algorithms after migration. Remove migrated authoritative entries and stale counts, narrow README and START_HERE-style material to an obvious algorithms learning path, update plans/reports/catalog/docs links, and clearly point learners to demo-data-structures. Retain algorithms that consume data structures only when they remain standalone and pedagogically algorithm-centered. Keep GoF/design-pattern material clearly labeled as deferred future extraction rather than mixing it into the main learning path. Add or update automated checks for broken paths, catalog drift, and duplicate ownership. Run the full demo-algorithms gate, commit, then complete.