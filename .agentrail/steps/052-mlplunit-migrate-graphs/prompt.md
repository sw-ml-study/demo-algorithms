Continue full native mlplunit adoption for all eight graph conformance files and nine graph demos. Extract shared production graph/algorithm definitions into appropriately bounded src/ files, make demos and tests use native include, remove copied implementations, and convert monolithic tests to focused named/tagged @test registry suites. Reuse lower-level included sources where safe, avoid name collisions, and use bracket fixtures only where graph setup/teardown ownership materially helps. Preserve cross-algorithm invariants, deterministic policies, zero-loop metadata, doc strings, catalogs, and outputs. Update migration inventory/docs and run human/TAP mlplunit, all demos, audits, and harness. Inspect but do not modify adjacent repositories; modify only this repository.