Extend the general-purpose sparse CSR source in this repository only with genuine sparse matrix composition. Implement dimension-checked CSR addition and sparse matrix-matrix multiplication, preserving deterministic sorted columns and zero elision, and add a practical mini-app such as composing sparse resource transformations. Use independent dense-oracle checks for small rectangular cases. Cover empty dimensions, identity/zero behavior, rectangular compatible products, incompatible dimensions, cancellation to zero, negative values, duplicate-free output, CSR invariants, and retained operands. Avoid pretending the pedagogical coordinate-domain scan is an optimized Gustavson implementation; document actual complexity and the future builders/grouping primitives needed. Prefer recursion/composition with no explicit loops, give every UDF a doc string, add focused native mlplunit tests, catalogs, README/docs/counts/sample reports, run just check, commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.