Move the three assertion-heavy vector scripts from demos/vectors into tests/vectors. Add a test catalog and runner support so these MLPL conformance tests remain registered and executable, while catalog/demos.tsv contains only result-oriented problem-solving demos. Update catalog schema/docs, plans, and references to define tests as pass/fail assertions and demos as mini-apps that describe a problem, use a data structure plus algorithm, and produce a meaningful result. Preserve all current test coverage and verify both suites.