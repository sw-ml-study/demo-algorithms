Add genuine N-queens and subset-sum backtracking mini-apps over numeric immutable state. For N-queens, solve a practical placement/constraint problem, return one deterministic row-by-row column placement or explicit unsatisfiable result, and validate attacks. For subset sum, solve a practical exact-budget/target selection problem, return deterministic selected indices/values without reusing items, and support signed values if practical with a bounded recursion policy. Add mlplunit coverage for N=0/1/2/3/4 and a larger known solution, deterministic first solution, row/column/diagonal invariants, retained state, invalid size policy; and subset empty/zero, impossible, exact singleton, multiple solutions/tie policy, duplicates, negative/zero values, no reuse, retained inputs, invalid target policy if any, selected-sum/index invariants, and known optimum/existence oracle. Prefer recursion and zero explicit loops; include meaningful doc strings for every u: function; document exponential worst-case work, pruning, recursion depth, immutable copy costs, and future modules/folds/generators/builders. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run full validation, and modify only this repository.