Implement honest functional Decorator and Proxy acceptance demos using named UDF references, fixed protocol records, and explicit immutable environment records available today. Keep edits only in this repository; adjacent sw-MLPL and mlplunit are read-only. Choose concrete general-purpose problems: Decorator must wrap/delegate behavior while adding validation or accounting without editing the wrapped function; Proxy must preserve a target protocol while controlling access or caching through explicit state/effects-as-data. Avoid hidden mutation and misleading opcode dispatch. Add doc-stringed shared production sources, result-oriented zero-loop demos, focused native mlplunit tests for substitutability, delegation, retained state/input, boundaries, and deterministic effects, catalog entries, documentation/count updates, regenerated human/TAP samples, and full validation. Clearly document any constraint caused by missing closures, partial binding, modules, dynamic callable collections, record update, or catchable callable-shape errors.