Continue full native mlplunit adoption for sets, hashing, maps, caches, and trees. Extract each reusable tested implementation to matching src/ files; make demos and tests include the production source; remove copied test implementations; split monolithic tests into focused named/tagged @test functions; use @cases only where clearer and bracket only for genuine lifecycle. Resolve helper-name collisions through sensible source boundaries pending full modules. Preserve doc strings, catalogs, demo outputs, immutable-version checks, and algorithm policies. Update migration inventory/docs and run human/TAP mlplunit, all demos, audits, and harness. Inspect but do not modify adjacent repositories; modify only this repository.