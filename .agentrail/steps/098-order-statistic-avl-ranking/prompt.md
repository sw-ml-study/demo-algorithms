Add a genuine rank/select mini-app using an immutable order-statistic AVL tree in this repository only. Extend or reuse the existing persistent AVL concepts with cached subtree sizes so an application can insert numeric keys, select the zero-based kth key, and compute the rank/insertion position of a key in sorted order. Demonstrate a practical problem such as live leaderboard placement while retaining an earlier snapshot. Cover empty/singleton trees, LL/RR/LR/RL rotations, duplicate policy, negative and zero keys, out-of-range select, missing-key rank semantics, cached height/size validation, BST ordering, balance bounds, and retained versions. Prefer recursion/composition with no explicit loops, give every UDF a doc string, distinguish logical O(log n) work from current nested-record copy costs and unguaranteed structural sharing, add focused native mlplunit tests, catalogs, README/docs/counts/sample reports, run just check, commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.