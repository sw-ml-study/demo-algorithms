Begin the numeric-associative-structures saga with a bit set and direct-address map. Create genuine numeric mini-apps that solve simple membership and key/value lookup problems plus mlplunit conformance tests. Cover empty state, insert/update/remove or clear, membership/lookup hit and miss, duplicate operations, bounds/invalid keys, negative-key policy, growth constraints, invariants, and retained immutable versions. Use array operations or recursion with explicit loop accounting, document bounded-universe tradeoffs and logical/current copy complexity, register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.