Create a queue conformance test under tests/queues and a genuine mini-app under demos/queues that solves a simple printer job scheduling problem. The demo must explain why FIFO queue ordering is fair, enqueue dynamically arriving numeric job IDs, dequeue jobs in arrival order, and print a meaningful processing result without assertions. The test must cover queue invariants, empty dequeue, FIFO order, dynamic growth/shrinkage, and persistence. Prefer pure functions and zero explicit loops if practical with current array operations. Register demo and test separately, update README, and run all suites. Keep helpers local until modules/imports land; do not modify ../sw-mlpl.