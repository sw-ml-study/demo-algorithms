Added a genuine stable task-ordering mini-app using recursive insertion sort over parallel priority and task-ID vectors with zero explicit loops. Insertion occurs after equal keys, visibly preserving FIFO arrival order. Added eleven mlplunit cases covering empty, singleton, already sorted, reverse sorted, interleaved duplicates with stability evidence, negative keys, and payload alignment. Documented O(n^2) comparisons and immutable copying, registered the eleventh demo/fourteenth test, updated README/plans, and passed the full validation matrix.