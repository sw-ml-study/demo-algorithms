Add a genuine maximum-flow mini-app using current sw-MLPL without modifying adjacent repositories. Implement deterministic Edmonds-Karp over a numeric capacity matrix using the existing queue/BFS idioms, residual forward and reverse capacities, augmenting-path reconstruction, and zero explicit loops. Solve a practical routing/allocation problem, return maximum flow plus inspectable flow/residual evidence, and test conservation, capacity bounds, source/sink edge cases, disconnected networks, parallel-capacity normalization policy, deterministic paths, retained inputs, and a known oracle. Explain logical O(V*E^2) work and immutable matrix-copy amplification. Update catalogs/docs/counts/sample reports, validate, commit, push, and identify language features that would simplify queue reuse and residual updates.