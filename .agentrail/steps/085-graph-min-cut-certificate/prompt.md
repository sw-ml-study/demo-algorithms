Extend the current Edmonds-Karp source with an inspectable minimum-cut certificate using only this repository. After maximum flow, traverse positive residual capacity from the source, return the source-side mask, deterministic crossing-edge evidence, and cut capacity, and prove max-flow equals min-cut on the classic and boundary fixtures. Build or extend the visible warehouse-transfer mini-app so the certificate solves a practical bottleneck-identification problem without turning it into a test dump. Add native tests for cut membership, crossing capacity, saturated forward cut edges, disconnected zero cuts, retained inputs, and malformed policies inherited from max flow. Prefer pure recursion and zero explicit loops. Document complexity/copy costs and language features that would simplify edge filtering and structured edge collections. Update catalogs/docs/counts/sample reports, validate, commit, and push.