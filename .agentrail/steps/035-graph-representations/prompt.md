Begin the graph-algorithms saga by implementing and comparing numeric directed-graph representations: adjacency matrix, edge list, and CSR. Create a genuine mini-app that models a small directed network and converts edge-list input to matrix and CSR, then answers deterministic neighbor/edge queries. Support zero-weight versus absent-edge semantics explicitly, duplicate-edge policy, self-loops, disconnected vertices, negative numeric vertex payloads if payloads are included, retained immutable source versions, and representation invariants. Add mlplunit coverage for empty/singleton graphs, directed asymmetry, duplicate edges, self-loops, isolated vertices, malformed endpoints, CSR offsets/targets, matrix/edge/CSR parity, and cycle-containing input as valid application data. Prefer pure recursion/array operations with zero explicit loops where practical; account honestly for loops and immutable copying. Document how numeric node IDs manage cycles, how modules will later share converters, and how weighted routing representations will extend this baseline. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.