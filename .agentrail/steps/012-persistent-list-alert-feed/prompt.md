Create a persistent immutable cons-list conformance test under tests/persistent_lists and a genuine mini-app under demos/persistent_lists that solves a simple newest-first alert-feed problem. New numeric alert IDs should be prepended without changing prior feed versions; traverse the current feed to print a meaningful result and retain an earlier snapshot to demonstrate persistence. Use nested records with an explicit empty sentinel, recursion or current control flow, no pointers/malloc/free, and document that current runtime cloning is semantic persistence without efficient structural sharing. The test must cover empty/singleton/multi-node traversal, prepend order, retained snapshots, and malformed cases practical today. Register demo/test, update README, run all suites, keep helpers local until modules/imports land, and do not modify ../sw-mlpl.