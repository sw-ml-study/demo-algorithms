Extend the persistent numeric BST map with immutable deletion and create a genuine mini-app that cancels reservations or removes inventory entries while retaining earlier roots. Implement and explain leaf deletion, one-child promotion, two-child replacement using inorder successor or predecessor, missing-key behavior, root deletion, stored zero and negative keys, duplicate/update policy compatibility, inorder ordering, size and BST invariants, repeated deletion to empty, and old-root validity. Add mlplunit coverage for every structural case and successor-path rebuilding. Prefer pure recursion and zero explicit loops; document logical O(height) work/allocation versus current nested-record copying and future structural sharing/GC. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.