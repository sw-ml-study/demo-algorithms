Add a genuine general-purpose sparse-matrix analytics mini-app in this repository only. Implement a numeric coordinate-list representation and deterministic conversion to validated CSR, sparse matrix-vector multiplication, and transpose or another clearly useful structural operation. Reuse graph CSR concepts only where appropriate but keep this demo owned by general matrix/data-structure use rather than graph traversal. Define duplicate coordinate policy and zero-elision policy. Cover empty and rectangular matrices, singletons, unsorted coordinates, duplicate entries, explicit zeros, negative values, dimension/index mismatches, CSR row-pointer/column-order/bounds invariants, dense-oracle comparisons, retained inputs, and zero explicit loops where current recursion/combinators permit. Give every UDF a doc string, document logical complexity and immutable array-copy amplification, add focused native mlplunit tests, catalogs, README/docs/counts/sample reports, run just check, commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.