Audit every tracked .mlpl file in demo-algorithms, demo-data-structures, and demo-design-patterns for required user-function doc strings and pedagogically necessary representation/invariant/complexity/copy/loop comments. Use ../sw-mlpl/scripts/mlpl-fmt.sh as the canonical formatter and format every .mlpl file in all three repositories. Add routine format and doc-string checks to each just pre-commit gate, while preserving the rule that web/*.mlpl is standalone and contains no include. Use failing-then-passing fixtures/checks, run all three mlplunit-backed gates, make detailed commits in each changed repo, dry-run push and push all three, and record exact commits.