Extend the Traveling Salesman routing source with deterministic 2-opt local improvement over a supplied closed tour, editing only this repository. Use recursive pair enumeration and segment reversal with no explicit loops. Validate closed tours and matrix edges, preserve the input tour, repeatedly accept a deterministic improving swap until locally stable, and prove the result never worsens nearest neighbor. Add a fixture where 2-opt strictly improves a nearest-neighbor seed if possible, while retaining the exact solver as an optimum oracle for small graphs. Update the existing demo/tests or add focused files, document O(V squared) per pass and immutable copying, update catalogs/counts/docs/samples, run full validation, commit, and push. Keep adjacent repositories read-only.