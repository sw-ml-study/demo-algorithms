Add a genuine longest-prefix routing mini-app using an immutable indexed numeric digit trie in this repository only. Because general strings are still immature, use vectors of decimal digits as keys and an indexed arena with a fixed ten-way child table per node. Support persistent insertion/replacement, exact lookup, and longest-prefix match for routing-like numeric codes; retain an earlier routing table. Define empty-key/root-value and duplicate policy explicitly. Cover empty trie, singleton, shared prefixes, prefix keys, missing exact keys, no-prefix match, zero digits, invalid digits/shapes, child-handle bounds, unreachable nodes or cycle validation as appropriate, and retained versions. Prefer recursion/composition with no explicit loops, give every UDF a doc string, document logical O(key length) operations versus current immutable array-copy amplification, add focused native mlplunit tests, catalogs, README/docs/counts/sample reports, run just check, commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.