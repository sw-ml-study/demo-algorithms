Added an immutable indexed decimal-digit trie using a flat ten-way child table, optional numeric values, persistent node append/link updates, empty-key root defaults, duplicate replacement, exact lookup, and longest-prefix lookup returning matched length. Added a genuine numeric account-routing mini-app retaining an earlier routing table and explicitly positioning digit vectors as the current workaround for immature general strings. Recursive validation covers shapes, binary flags, integral handle bounds, reachability, cycles, and shared children. Three native mlplunit cases cover empty/singleton/root/shared/prefix/zero-digit keys, exact misses, fallback/no-prefix behavior, replacements, retained versions, invalid digits/shapes/values, and graph corruption. Updated catalogs, README, analysis, plan, counts, and human/TAP reports. Full just check passed: 84 demos, 78 test files, 151 native tests/cases, and 829 documented UDFs.