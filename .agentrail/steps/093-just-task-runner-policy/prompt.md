Add a repository-local justfile as the preferred task entry point, delegating to existing scripts rather than duplicating logic. Explicitly document in README and hand-editable project guidance in AGENTS.md that just is preferred and Makefiles/make should not be introduced unless the user explicitly requests them. Do not edit the AgentRail-generated instruction block; add project-specific guidance outside it. Clarify that no CLAUDE.md currently exists and avoid duplicating instructions into a new file unless needed. Include useful recipes for demos, tests, TAP, list, focused test arguments, harness/audits, and an aggregate check. Verify just is installed and recipes work, retain direct script commands for portability, commit, complete, and push before resuming Robin Hood deletion.