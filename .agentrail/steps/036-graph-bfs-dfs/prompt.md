Add genuine BFS and DFS mini-apps over the current numeric graph representation, solving reachability/order problems on directed graphs with cycles and disconnected vertices. BFS must delegate to a copied-local pure queue contract and DFS to recursive traversal or an explicit copied-local stack; explain that modules will remove this duplication. Produce deterministic visitation order based on edge/neighbor order. Add mlplunit coverage for empty or invalid start policy, singleton, chain, branching graph, directed cycle, self-loop, duplicate-normalized edges, disconnected/unreachable vertices, full-forest traversal if included, BFS shortest-hop levels or parents, DFS preorder, visited-mask termination, and retained graph versions. Prefer recursion and zero explicit loops where practical, account honestly for any loops and immutable queue/stack copying, register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.