Add a deterministic numeric integer-mixing/hash foundation with golden fixtures suitable for the upcoming open-address map. Create a genuine mini-app showing distribution of ordinary numeric IDs into a bounded bucket table and an mlplunit test covering determinism, negative-key policy or normalization, boundary values, bucket range, known golden outputs, and collision examples. Avoid relying on unstable platform hash behavior; document numeric precision/integer constraints and why this is pedagogical rather than cryptographic. Prefer whole-array operations or recursion with loop accounting, register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.