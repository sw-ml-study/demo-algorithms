Create demos/vectors/chunked_vector.mlpl demonstrating a dynamically sized chunked numeric vector using a rank-2 chunk matrix, logical size, capacity, and fixed chunk width. Implement pure append with in-capacity update and capacity growth, bounds-safe read, representation validation, and retained-old-version tests. Keep it standalone and deterministic, use no explicit loops if current whole-array operations suffice, document logical versus current copy complexity, register it in catalog/demos.tsv, and run the full harness/catalog/demo suite against the adjacent release binary. Do not modify ../sw-mlpl.