Move the manifest-approved sequential structures from demo-algorithms to demo-data-structures: array memory/vector variants, stacks, queues, deques, linked lists, persistent lists, and directly associated demos/tests/docs. Preserve shared src definitions, deterministic boundary/empty/singleton/duplicate/invalid coverage, complexity/copy/loop documentation, catalog behavior, and user-facing learning order. Add failing catalog/ownership assertions before or with migration changes. Run both repositories' scoped gates, commit each repository separately, and record both commits before completing.