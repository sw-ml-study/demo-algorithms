Investigate and implement honest fixed-schema functional Command and Visitor acceptance demos using current named UDF references and fixed records. Keep edits only in this repository; adjacent sw-MLPL and mlplunit are read-only. Command must carry executable behavior plus explicit numeric arguments/environment and return immutable state/effects, demonstrating invocation and retained prior state without pretending a dynamic heterogeneous history exists. Visitor must separate at least two operation algebras from the existing closed numeric expression-tree structure, using a fixed record of callable operations and recursion; document whether missing UDF-capable folds or recursive general values constrain the preferred form. Add doc-stringed shared production sources, result-oriented zero-loop demos, focused native mlplunit tests, catalogs, documentation/count updates, regenerated human/TAP samples, and full validation. If either pattern cannot be implemented honestly, publish the exact executable gate and continue with the other.