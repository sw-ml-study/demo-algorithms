Add an all-pairs Floyd-Warshall mini-app over a weighted directed graph with explicit presence/infinity semantics and path reconstruction if practical. Cross-check each source row against Bellman-Ford or the prior single-source fixture where policies overlap. Support zero-weight and negative edges without negative cycles, detect negative cycles through diagonal results, preserve unreachable pairs, and use deterministic next-hop or predecessor policy. Add mlplunit coverage for empty/singleton, direct versus multi-hop paths, unreachable pairs, zero edge, negative edge, positive cycle, negative cycle, asymmetric directed routes, path reconstruction, matrix dimensions/indexing, retained graph versions, malformed endpoints, and parity with known single-source distances. Prefer recursion and zero explicit loops; document O(V^3) work, immutable matrix-copy overhead, and future array-update/builder improvements. Register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.