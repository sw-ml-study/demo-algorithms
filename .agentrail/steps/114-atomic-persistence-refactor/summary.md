Verified sw-MLPL 0.20.0 binary/source 2c7806a4 and replaced both repository persistence paths with write_atomic. JSON now atomically replaces deterministic UTF-8 documents; device commands atomically replace numeric-byte packets. Added focused tests that seed longer prior files, replace them with shorter values, prove exact content/no stale suffix, reload successfully, and clean up. Updated README and serialization/status/plan documents to mark same-filesystem temp-plus-rename replacement shipped while retaining byte-kind, Result validation, JSON semantic, limits, TOML, native format, and streaming blockers. just check passes: 92 demos, 85 test files, 176 native tests, 949/949 documented UDFs.