Using verified sw-MLPL 0.20.0 binary/source 2c7806a4 and editing only this repository, replace non-atomic persistence in the JSON and raw-byte demos with write_atomic. Add focused mlplunit evidence that an existing longer JSON/string or byte file is replaced completely and reloads correctly, with cleanup. Refresh README and serialization/status/plan documents to mark crash-safe same-filesystem replacement shipped and retain exact caveats. Run just check, commit, complete the maintenance step, and push.