Add explicit directed graph cycle-detection and topological-sort mini-apps over numeric node IDs. Cycle detection should use a color/recursion-stack contract and return deterministic evidence of a cycle if practical; topological sort should use Kahn indegrees and a pure queue, returning Err when a cycle prevents a complete order. Demonstrate both a valid dependency DAG and valid application graph containing self-loops or longer cycles, documenting that detection reports cycles rather than forbidding construction. Add mlplunit coverage for empty, singleton acyclic, self-loop, two/three-node cycles, disconnected components, DAG branching/ties with deterministic policy, topological edge-order validity, cycle rejection, duplicate-normalized edges, malformed endpoint policy, and retained graph versions. Prefer recursion and zero explicit loops where practical; document immutable copying and future module reuse, register scripts, update README/plans/capability evidence, inspect but do not modify adjacent repositories, run the full validation matrix, and modify only this repository.