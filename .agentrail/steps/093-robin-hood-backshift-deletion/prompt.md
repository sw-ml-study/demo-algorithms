Extend the Robin Hood numeric hash map in this repository only with immutable backward-shift deletion. Removing a key must close the probe-chain hole by shifting following residents backward until an empty slot or zero-distance resident, decrementing stored distances and preserving early-termination lookup; do not use tombstones. Update the sensor-registry mini-app to solve a genuine unregister/re-register problem while retaining a prior snapshot. Add focused native tests for missing deletion, home/displaced/wraparound deletion, shifted lookup preservation, stored zero, re-insertion, full-to-nonfull transition, invariants, deterministic layout, and retained versions. Explain logical O(cluster length), current immutable copy amplification, and comparison with the existing tombstone demo. Update catalogs/README/docs/counts/samples, validate, commit, and push. Do not edit sibling repositories.