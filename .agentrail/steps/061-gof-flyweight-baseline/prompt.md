Continue the functional GoF baseline saga with Flyweight. Build a genuine problem-solving .mlpl mini-app that separates shared intrinsic numeric data in one table from many lightweight numeric IDs plus per-use extrinsic state, with focused native mlplunit tests using shared src definitions. Demonstrate reuse, lookup/validation, retained shared data, and meaningful memory-model reasoning without claiming runtime interning or physical sharing that cannot be observed. Use zero explicit loops where practical; document logical/current copy complexity, recursion, limitations, and why the representation preserves Flyweight intent without OO objects. Update catalogs, README, DESIGN_PATTERNS.md, analysis/plan/saga docs, and sample reports. Modify only this repository and run full validation.