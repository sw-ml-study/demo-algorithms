Continue the functional GoF baseline saga with Prototype. Build a genuine problem-solving .mlpl mini-app that derives modified graph or configuration values from an existing prototype while retaining and demonstrating the original version, plus focused native mlplunit tests using shared src definitions. Prefer pure cloning-by-value plus explicit transformations, distinguish semantic value copying from unavailable physical structural-sharing evidence, use zero explicit loops where practical, and document logical/current copy complexity, recursion, limitations, and why this preserves Prototype intent without OO clone methods. Reuse existing source definitions where appropriate; update catalogs, README, DESIGN_PATTERNS.md, analysis/plan/saga docs, and sample reports. Modify only this repository and run full validation.