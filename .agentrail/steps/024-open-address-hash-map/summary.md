Added a genuine sparse-meter mini-app using a fixed-capacity open-address numeric hash map with deterministic hashing and recursive linear probing. Demonstrated intentional collisions, three-probe lookup, duplicate-key update, and retained versions. Added seventeen mlplunit cases covering empty lookup, insertion, collision chains, wraparound layout, stored zero, negative keys, update size, invalid keys, full-table insert failure, bounded full-table missing lookup, invariants, and immutability. Used zero explicit loops; explicitly deferred deletion/tombstones and resizing. Registered eighteen demos/twenty-one tests, updated README/plans, and passed the full validation matrix.