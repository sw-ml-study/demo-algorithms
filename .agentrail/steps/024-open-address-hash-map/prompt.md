Add a genuine fixed-capacity numeric hash map using open addressing with linear probing and the deterministic mixer. Create a mini-app solving sparse numeric ID lookup/update and an mlplunit test covering empty lookup, insert, duplicate-key update, collision chains, wraparound, missing keys, full-table failure, negative keys within policy, stored zero, invariants, and retained immutable versions. Do not implement deletion/tombstones or resizing yet; document those as the next gated steps. Prefer recursion with zero explicit loops if practical, account for probe termination and current copy costs, register scripts, update README/plans, inspect current mlplunit behavior, run the full validation matrix, and do not modify adjacent repositories unless a concrete blocker is first reported.