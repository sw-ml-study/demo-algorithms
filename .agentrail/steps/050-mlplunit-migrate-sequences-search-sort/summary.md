Migrated all 16 vector/stack/queue/linked-list/persistent-list/search/sequence/sort/heap test files to native mlplunit. Promoted stricter test implementations into 16 documented src/ production files, made every paired demo include those sources, removed all copied production functions from migrated tests, and converted each to named/tagged @test registry execution. Added genuine reflected @cases coverage for fixed-width lower-bound rows and intentionally omitted bracket fixtures because these immutable algorithms own no lifecycle. Resolved five concrete demo/test API drifts in favor of Result-returning and unified keys/IDs contracts. Mechanical audit shows migrated tests define only test/case callables. Updated adoption status to 17/41 complete, 24 remaining. Full validation passes: 40 demos, 41 human/TAP tests, 608/608 documented functions, and harness contract.