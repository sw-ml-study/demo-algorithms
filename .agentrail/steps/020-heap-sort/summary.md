Added a genuine shortest-batch-first mini-app using heap sort delegated to locally copied dense min-heap operations. Implemented recursive heap build, sift-up/down, remove-min, and drain with zero explicit loops and no sorting builtin. Added eight mlplunit cases covering empty, singleton, sorted, reverse sorted, duplicates, negatives, mixed values, and input immutability. Documented logical O(n log n) work, immutable copy costs, and concrete module pressure from duplicated heap helpers; registered the fourteenth demo/seventeenth test, updated README/plans, and passed the full validation matrix.