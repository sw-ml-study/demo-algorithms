Add a genuine scheduling-conflict mini-app using an immutable augmented interval tree in this repository only. Store numeric half-open intervals in a balanced tree (prefer an AVL representation) with cached subtree maximum endpoint, support persistent insertion and return one overlapping interval or no match without flattening the tree. Define endpoint and duplicate-start policy explicitly. Demonstrate practical appointment or resource-reservation conflict detection while retaining an earlier schedule. Cover empty/singleton trees, touching non-overlapping boundaries, nested and duplicate intervals, negative and zero endpoints, left/right pruning paths, all rotation families if AVL-based, malformed intervals, cached max/height validation, BST ordering, balance, and retained roots. Prefer recursion/composition with no explicit loops, give every UDF a doc string, distinguish logical search/insert complexity from current nested-record copies and unguaranteed structural sharing, add focused native mlplunit tests, catalogs, README/docs/counts/sample reports, run just check, commit, complete, push, and select the next non-memory-oriented step. Do not edit sibling repositories.