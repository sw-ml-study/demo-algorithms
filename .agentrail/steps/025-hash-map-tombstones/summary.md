Extended the fixed-capacity open-address map with tombstone deletion and first-tombstone reuse. The mini-app removes a colliding meter without breaking later chain lookup, then safely reuses the deleted slot. Added seventeen mlplunit cases for chain/wraparound preservation, remove hit/miss, reuse, duplicate update, stored zero, negative/invalid keys, full and tombstoned-table termination, size/invariants, and retained versions, all with zero explicit loops. Also made doubly linked insert/delete/traversals, graph cycle detection/SCC, Dijkstra path reconstruction, A*, TSP exact/approximate routing, and research-inspired hashing comparisons explicit in the plan. Registered nineteen demos/twenty-two tests and passed the full validation matrix.